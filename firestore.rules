// firestore.rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function signedIn() {
      return request.auth != null;
    }

    // 臨時開發規則：只要登入就可讀寫（上線驗收用）
    match /{document=**} {
      allow read, write: if signedIn();
    }
  }
}
